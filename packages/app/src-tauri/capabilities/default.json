{
  "identifier": "default",
  "description": "기본 앱 권한",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-open",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
